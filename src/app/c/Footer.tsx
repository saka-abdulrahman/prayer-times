export default function Footer({ isDark, language }) {
  return (
    <footer
      className={` absolute bottom-0 flex items-center justify-center w-full`}
    >
      <h1 className={` text-2xl p-2 `}>©Abdulrahman-Saka</h1>
    </footer>
  );
}
